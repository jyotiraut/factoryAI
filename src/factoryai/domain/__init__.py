"""Domain layer: entities, value objects, ports and policies.

This package must remain free of any framework or infrastructure import. It depends only
on the standard library and Pydantic. The restriction is enforced by the import-linter
contracts declared in ``pyproject.toml``.

Populated in Phase 1 (see ``docs/ROADMAP.md``).
"""
